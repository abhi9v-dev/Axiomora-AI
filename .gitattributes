# Normalize line endings to LF in the repository regardless of platform,
# so diffs stay clean across Windows/macOS/Linux contributors.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.xlsx binary
